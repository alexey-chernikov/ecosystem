- name: Install apt packages
  become: yes
  ansible.builtin.apt:
    pkg:
      - apache2-utils

- name: Install snap packages
  become: yes
  community.general.snap:
    name: 
      - kubectl
      - helm
    classic: true

